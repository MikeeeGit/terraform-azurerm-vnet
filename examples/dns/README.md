# DNS composition

Demonstrates the original public/private A, CNAME and apex MX inputs. The private zone is linked once to the VNet with registration disabled. These reserved example names and documentation IPs are illustrative; replace them before a real deployment. Creating a public Azure zone does not configure registrar delegation.
